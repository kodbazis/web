<h1 class="p-3 border-bottom">
	Webfejlesztő online kurzusok
</h1>
<div class="row m-auto mb-4">
	{% for item in items %}
		{% if (item.id is not 3) %}

			<div class="col-md-6">
				<a href="{{item.course.slug}}">
					<div class="card p-2 m-2">

						<img src="/public/files/md-{{item.course.imgUrl}}" class="w-100"/>

						<p class="lead p-2 text-center text-dark" style="min-height: 130px">
							<b>{{item.course.description}}</b>
						</p>

						<div class="col-md-12 border bg-white">
							<p class="text-center pt-1 text-dark">
								<b>A teljes képzés ára:</b>
							</p>
							{% if item.course.price == item.discountedPrice %}
								<h1 class="p-3 no-discount" style="font-size: 3em; text-shadow: 2px 3px 5px #ccc;">
									<b>{{item.course.price | number_format(0, '', ' ')}}
										Ft</b>
								</h1>
							{% else %}
								<div class="row p-2">
									<div class="col-md-8" style="text-shadow: 2px 3px 5px #ccc;">
										<span class="first">{{item.discountedPrice | number_format(0, '', ' ') }}
											Ft</span>
										<span class="second strikethrough">{{item.course.price | number_format(0, '', ' ')}}
											Ft</span>
									</div>
									<div class="col-md-4">

										<div class="float-right text-center">
											<div class="third text-center">
												-{{item.course.discount}}%
											</div>
											<span style="font-size: 10px">Limitált ideig!</span>

										</div>
									</div>


								</div>

							{% endif %}
							<button class="_3jJ8O sc-htpNat fMQhWV mt-2" format="primary" id="login_signup_button" type="submit" tabindex="5">
								<span class="sc-bxivhb jrbrfM">
									Érdekel
									<i class="fa fa-arrow-circle-right"></i>
								</span>
							</button>
						</div>
					</div>
				</a>
			</div>
		{% endif %}
	{% endfor %}
</div>
